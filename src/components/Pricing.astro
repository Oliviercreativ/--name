---
interface Props {
    title?: string;
    subtitle?: string;
    plans: {
        name: string;
        price: number;
        description: string;
        features: string[];
        popular?: boolean;
        buttonText: string;
        buttonAction: string;
    }[];
    guarantee?: {
        text: string;
        icon?: string;
    };
}

const {
    title = "Choisissez votre formule",
    subtitle = "Apprenez à le créer ou on s'en charge pour vous",
    plans = [],
    guarantee = {
        text: "Garantie \"Opérationnel ou Remboursé\" 30 jours",
        icon: "🛡️"
    }
} = Astro.props;
---

<section id="formules" class="bg-gray-50 py-20 lg:py-32">
    <div class="max-w-6xl mx-auto px-4 sm:px-6">
        <div class="text-center mb-20">
            <h2 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-6">
                {title}
            </h2>
            <p class="text-xl text-gray-600">
                {subtitle}
            </p>
        </div>

        <div class="grid lg:grid-cols-{plans.length} gap-8 max-w-4xl mx-auto">
            {plans.map((plan, index) => (
                <div class={`bg-${plan.popular ? 'zest-500' : 'white'} rounded-2xl p-8 ${plan.popular ? 'text-white' : 'border border-gray-200'} relative`}>
                    {plan.popular && (
                        <div class="absolute -top-3 left-1/2 transform -translate-x-1/2">
                            <span class="bg-white text-zest-500 px-4 py-1 rounded-full text-sm font-semibold">
                                Populaire
                            </span>
                        </div>
                    )}
                    
                    <div class="text-center mb-8">
                        <h3 class="text-2xl font-bold mb-2">{plan.name}</h3>
                        <div class="text-4xl font-bold mb-2">{plan.price}€</div>
                        <div class={plan.popular ? 'text-zest-100' : 'text-gray-600'}>
                            {plan.description}
                        </div>
                    </div>
                    
                    <ul class="space-y-4 mb-8">
                        {plan.features.map((feature) => (
                            <li class="flex items-center space-x-3">
                                <div class={`w-5 h-5 ${plan.popular ? 'bg-white/20' : 'bg-green-100'} rounded-full flex items-center justify-center`}>
                                    <span class={`${plan.popular ? 'text-white' : 'text-green-600'} text-xs font-bold`}>✓</span>
                                </div>
                                <span>{feature}</span>
                            </li>
                        ))}
                    </ul>
                    
                    <button 
                        onclick={`selectPlan('${plan.buttonAction}')`}
                        class={`w-full ${plan.popular ? 'bg-white text-zest-500 hover:bg-gray-100' : 'bg-gray-100 text-gray-900 hover:bg-gray-200'} py-3 rounded-lg font-semibold transition-colors`}
                    >
                        {plan.buttonText}
                    </button>
                </div>
            ))}
        </div>

        {guarantee && (
            <div class="text-center mt-16">
                <div class="inline-flex items-center space-x-2 bg-green-50 px-6 py-3 rounded-full">
                    {guarantee.icon && <span class="text-green-600 font-bold">{guarantee.icon}</span>}
                    <span class="text-green-800 font-semibold">{guarantee.text}</span>
                </div>
            </div>
        )}
    </div>
</section>

<script>
    function selectPlan(plan: string) {
        // Logique de sélection du plan
        console.log('Plan sélectionné:', plan);
        // Redirection ou ouverture du formulaire de contact
    }
</script> 